# 账号绑定补全任务卡

## 遗漏项

**已绑定 user_id（没问题）：**
1. ✅ 魔城币 — `user://{user_id}_mocheng_coin.json`
2. ✅ 解锁状态 — `user://{user_id}_meta_progression.json`
3. ✅ 每日计数器 — `user://{user_id}_daily_counters.json`

**未绑定 user_id（需要修复）：**
1. ⚠️ 爬塔存档 — `user://saves/save_001.json`（全局文件，所有用户共用）
2. ⚠️ 档案文件 — `user://archive.json`（全局文件）
3. ⚠️ 玩家数据 — `user://saves/player_data.json`（全局文件）

---

## 修复步骤

### Bug1：爬塔存档绑定 user_id

**文件：`autoload/save_manager.gd`**

找到 `save_run_state()` 和 `load_latest_run()`，修改为带 user_id 前缀：

```gdscript
func save_run_state(run_data: Dictionary, is_auto: bool = true, slot_id: int = 1, user_id: String = current_user_id) -> bool:
    var file_path: String = ConfigManager.SAVE_DIR + "%s_save_%03d.json" % [user_id, slot_id]
    ...

func load_latest_run(user_id: String = current_user_id) -> Dictionary:
    var file_path: String = ConfigManager.SAVE_DIR + "%s_save_%03d.json" % [user_id, 1]
    if not FileAccess.file_exists(file_path):
        return {}
    ...

func has_active_run(user_id: String = current_user_id) -> bool:
    var file_path: String = ConfigManager.SAVE_DIR + "%s_save_%03d.json" % [user_id, 1]
    return FileAccess.file_exists(file_path)
```

**同时检查调用方：**
- `run_controller.gd` `_auto_save()` 调用 `save_run_state()` 时不需要改（默认用 current_user_id）
- `run_controller.gd` `_save_at_floor_entrance()` 同上
- `menu.gd` 检查 "继续游戏" 按钮时调用 `has_active_run()` 不需要改

### Bug2：档案文件绑定 user_id

**文件：`autoload/config_manager.gd`**

找到 `ARCHIVE_FILE`，改为动态路径：

```gdscript
# 原：const ARCHIVE_FILE: String = "user://archive.json"
# 改为：
func get_archive_file_path(user_id: String = SaveManager.get_user_id()) -> String:
    return "user://%s_archive.json" % user_id
```

所有引用 `ARCHIVE_FILE` 的地方改为调用 `get_archive_file_path()`。

### Bug3：玩家数据绑定 user_id

**文件：`autoload/save_manager.gd`**

```gdscript
func load_player_data(user_id: String = current_user_id) -> Dictionary:
    var file_path: String = ConfigManager.SAVE_DIR + "%s_player_data.json" % user_id
    if not FileAccess.file_exists(file_path):
        return _create_default_player_data()
    
    var file := FileAccess.open(file_path, FileAccess.READ)
    var json := JSON.new()
    json.parse(file.get_as_text())
    file.close()
    return json.get_data()

func save_player_data(data: Dictionary, user_id: String = current_user_id) -> void:
    var file_path: String = ConfigManager.SAVE_DIR + "%s_player_data.json" % user_id
    var file := FileAccess.open(file_path, FileAccess.WRITE)
    file.store_string(JSON.stringify(data))
    file.close()

func _create_default_player_data() -> Dictionary:
    return {
        "first_login": Time.get_unix_time_from_system(),
        "total_play_time": 0,
        "total_runs": 0,
        "total_victories": 0
    }
```

### Bug4：影子池绑定 user_id（可选，如果 shadow_pool.json 是全局的）

**文件：`scripts/systems/virtual_archive_pool.gd`**

```gdscript
func get_shadow_pool_file_path(user_id: String = SaveManager.get_user_id()) -> String:
    return "user://%s_shadow_pool.json" % user_id

func save_shadows_to_disk() -> void:
    var file_path: String = get_shadow_pool_file_path()
    ...

func load_shadows_from_disk() -> void:
    var file_path: String = get_shadow_pool_file_path()
    ...
```

---

## 验收标准
- [ ] 切换 user_id 后，"继续游戏"加载的是新用户的存档（不是旧用户的）
- [ ] 切换 user_id 后，档案文件独立（不看到其他用户的档案）
- [ ] 切换 user_id 后，玩家数据独立（游玩时间、局数等不混淆）
- [ ] 新用户首次登录时，所有文件自动创建默认值
- [ ] 旧用户（本地单机版）数据通过 `_migrate_*` 函数迁移到新格式
