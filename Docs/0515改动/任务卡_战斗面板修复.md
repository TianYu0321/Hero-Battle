# 暗黑风格战斗面板修复任务卡

## 问题分析

当前暗黑风格战斗面板存在3个关键问题：

### 问题1：敌人信息卡片完全缺失（右上空白）
**根因**：`run_main.gd` 调用 `battle_animation_panel.start_battle(enemy_cfg, hero_data)` 传了**两个参数**，但 `battle_animation_panel.gd` 的 `start_battle()` 只接收**一个字典参数**。GDScript 会把 `enemy_cfg` 当作 `battle_result` 字典，但 `enemy_cfg` 的键名和 `battle_result` 期望的键名不匹配（`enemy_cfg` 里有 `name`/`max_hp`/`hp`，但 `start_battle` 期望的是 `enemy_name`/`enemy_max_hp`/`enemy_hp`）。

**修复**：修改 `run_main.gd` 的 `_start_full_battle_ui()`，把 `enemy_cfg` + `hero_data` 合并成符合 `start_battle()` 期望的 `battle_result` 字典。

### 问题2：PVP大厅背景按钮穿透可见
**根因**：`SemiTransparentBg` 的 alpha 只有 0.7，PVP 大厅底层按钮文字清晰透出。且 `battle_animation_panel.gd` 缺少 PVP 大厅需要的 `start_playback()` 方法。

**修复**：
1. 背景 alpha 从 0.7 提高到 0.85，底层更模糊
2. 在 `battle_animation_panel.gd` 里**加回 `start_playback()` 方法**，兼容 PVP 大厅的调用（`start_playback(recorder, hero_name, enemy_name, hero_max_hp, enemy_max_hp, [], [])`）
3. `start_playback()` 内部把 recorder 数据转换成 `battle_result` 字典，复用现有的暗黑风格显示逻辑

### 问题3：敌人舞台剪影不显示（右下空白）
**根因**：同问题1，敌人数据没正确传入，导致 `EnemyArt` 没有任何内容显示。

**修复**：随问题1修复自动解决。

---

## 修改清单

### 文件1：`scenes/run_main/run_main.gd`

**修改 `_start_full_battle_ui()`**：

```gdscript
func _start_full_battle_ui() -> void:
	if _run_controller == null or _combat_selected_index < 0:
		return
	
	var summary: Dictionary = _run_controller.get_current_run_summary()
	var node_options: Array[Dictionary] = summary.get("node_options", [])
	if _combat_selected_index >= node_options.size():
		return
	
	var selected_node: Dictionary = node_options[_combat_selected_index]
	var enemy_cfg: Dictionary = selected_node.get("enemy_config", {})
	var hero_data: Dictionary = summary.get("hero", {})
	
	# 构建 battle_result 字典，符合 battle_animation_panel.start_battle() 的期望
	var battle_result: Dictionary = {
		"hero_name": hero_data.get("hero_name", "英雄"),
		"enemy_name": enemy_cfg.get("name", "???"),
		"hero_max_hp": hero_data.get("max_hp", 100),
		"enemy_max_hp": enemy_cfg.get("max_hp", 100),
		"hero_hp": hero_data.get("current_hp", hero_data.get("max_hp", 100)),
		"enemy_hp": enemy_cfg.get("hp", enemy_cfg.get("max_hp", 100)),
		"total_rounds": enemy_cfg.get("estimated_hp_loss", 10) / 5 + 2,
		"stage_name": selected_node.get("node_name", "深渊斗技场"),
	}
	
	_show_modal_panel(battle_animation_panel)
	battle_animation_panel.start_battle(battle_result)
	
	call_deferred("_execute_combat_deferred", _combat_selected_index)
	_combat_selected_index = -1
	_is_combat_preview = false
```

### 文件2：`scenes/run_main/battle_animation_panel.gd`

**1. 修改 `_apply_dark_theme()` — 提高背景不透明度：**
```gdscript
func _apply_dark_theme() -> void:
	semi_transparent_bg.color = Color(0.05, 0.05, 0.08, 0.85)  # alpha从0.7提高到0.85
	# ...其余不变
```

**2. 新增 `start_playback()` 方法（兼容 PVP 大厅）：**
```gdscript
## 兼容 PVP 大厅的旧版调用
func start_playback(recorder, hero_name: String, enemy_name: String,
					hero_max_hp: int, enemy_max_hp: int,
					_hero_partners: Array, _enemy_partners: Array) -> void:
	# 从 recorder 提取回合数
	var total_rounds: int = 1
	if recorder != null and recorder.has_method("get_events"):
		var events = recorder.get_events()
		total_rounds = maxi(1, events.size() / 2)
	
	var battle_result: Dictionary = {
		"hero_name": hero_name,
		"enemy_name": enemy_name,
		"hero_max_hp": hero_max_hp,
		"enemy_max_hp": enemy_max_hp,
		"hero_hp": hero_max_hp,  # PVP战斗结束后由外部更新
		"enemy_hp": 0,
		"total_rounds": clampi(total_rounds, 3, 20),
		"stage_name": "PVP 决斗场",
	}
	start_battle(battle_result)
```

**3. 修改 `start_battle()` — 增加数据校验和默认值：**
```gdscript
func start_battle(battle_result: Dictionary) -> void:
	_playback_generation += 1
	_result_emitted = false
	_is_playing = true
	visible = true
	
	_total_rounds = maxi(1, battle_result.get("total_rounds", 1))
	_current_round = 0
	
	_hero_max_hp = maxi(1, battle_result.get("hero_max_hp", 100))
	_hero_hp = clampi(battle_result.get("hero_hp", _hero_max_hp), 0, _hero_max_hp)
	_enemy_max_hp = maxi(1, battle_result.get("enemy_max_hp", 100))
	_enemy_hp = clampi(battle_result.get("enemy_hp", _enemy_max_hp), 0, _enemy_max_hp)
	
	var hero_name: String = battle_result.get("hero_name", "英雄")
	var enemy_name: String = battle_result.get("enemy_name", "???")
	var stage_name: String = battle_result.get("stage_name", "深渊斗技场")
	
	hero_name_label.text = hero_name
	enemy_name_label.text = enemy_name
	stage_name_label.text = stage_name
	
	# 确保舞台剪影可见
	hero_art.visible = true
	enemy_art.visible = true
	
	_update_hp_display()
	
	hero_hp_bar.add_theme_color_override("theme_fg", COL_BLUE_MAIN)
	hero_hp_bar.add_theme_color_override("theme_bg", COL_BLUE_DEEP)
	enemy_hp_bar.add_theme_color_override("theme_fg", COL_RED_MAIN)
	enemy_hp_bar.add_theme_color_override("theme_bg", COL_RED_DEEP)
	
	battle_log.text = ""
	battle_log.append_text("[color=#E6C040]战斗开始！[/color]\n")
	
	print("[BattleAnimation] 开始: gen=%d, %d回合, 英雄=%s, 敌人=%s" % [
		_playback_generation, _total_rounds, hero_name, enemy_name
	])
	_play_turn()
```

### 文件3：`scenes/run_main/battle_animation_panel.tscn`

**修改 `SemiTransparentBg` 的 color：**
```
color = Color(0.05, 0.05, 0.08, 0.85)
```

---

## 测试要点

1. **爬塔战斗**：点击「战斗」→ 预览浮层 → 进入战斗 → 确认左上和右上都有卡片（英雄+敌人），中央 VS 徽章，底部日志正常滚动
2. **PVP 战斗**：在 PVP 大厅点击「PVP 对决」→ 战斗面板弹出 → 确认左右卡片都有数据，背景足够暗（底层按钮模糊不可读）
3. **跳过功能**：点击「跳过」→ 动画直接结束，不报错
4. **返回功能**：点击「返回」→ 预览浮层关闭，选项按钮恢复

---

## 注意事项

- 不要修改 `pvp_lobby.gd` 或 `pvp_lobby.tscn`，PVP 大厅的调用方式保持不变
- 不要修改菜单、商店、排行榜等其他场景
- 只改 `run_main.gd`、`battle_animation_panel.gd`、`battle_animation_panel.tscn` 三个文件